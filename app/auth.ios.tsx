
// app/auth.ios.tsx
// iOS-specific auth screen (currently identical to base auth.tsx)
// Biometric authentication has been removed as it's not currently needed
import AuthScreen from './auth';

export default AuthScreen;
